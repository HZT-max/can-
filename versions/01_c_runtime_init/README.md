# 01：加入标准 C 运行时初始化

相对已验证的 `00`，只增加：

1. 从 Flash 把 `.data` 初值复制到 SRAM。
2. 把 SRAM 中的 `.bss` 清零。
3. 检查一个 `.data` 标记和一个 `.bss` 标记。

PB1、默认 HSI 时钟、延时循环和闪灯方式均保持不变。

观察结果：

- RUN 持续闪烁：C 运行时初始化通过。
- RUN 常亮：`.data` 或 `.bss` 初始化检查失败。
- RUN 不亮：程序没有执行到 GPIO 检查位置。

烧录文件为 `firmware.bin`，地址为 `0x08000000`。
