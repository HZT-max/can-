# 02：修正 `.data` 的 Flash 装载地址对齐

相对失败的 `01`，只在链接脚本 `.text` 末尾增加：

```ld
. = ALIGN(4);
```

这样紧随其后的 `.data` 初值在 Flash 中从 4 字节边界开始，Cortex-M0 可以安全执行编译器生成的 32 位读取指令。

其余源码、PB1、默认 HSI 时钟、延时循环和运行时初始化逻辑全部保持不变。

实物结果：通过，RUN 持续闪烁。

烧录文件为 `firmware.bin`，地址为 `0x08000000`。
